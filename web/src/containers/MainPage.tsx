import * as React from 'react';
import { Registration } from '../components/RegistrationBtn';

export const MainPage = () => {
  return (
    <React.Fragment>
      <Registration />
    </React.Fragment>
  );
};
